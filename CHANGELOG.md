# Changelog

## 6.2.0 - 2026-08-06

- BOSS 自动消息的岗位来源改为紧凑标签页，支持原有推荐模式和新的检索模式；检索模式通过 `POST /wapi/zpgeek/search/joblist.json` 按关键词、城市、岗位类型、薪资、经验、学历、行业、规模、融资阶段、职位类型、区域或地铁查询岗位。
- BOSS 推荐模式新增城市、求职类型、推荐薪资、工作经验、学历、行业和公司规模筛选；多选参数保留字面逗号，任务启动后分页只修改 `page`，不会重新编码或覆盖任务筛选快照。
- BOSS 城市、通用条件、行业和职位类型元数据按 30 天有效期缓存在 `chrome.storage.local`；区域和地铁按所选城市动态加载，使用层级选项、父子互斥，并且两类位置条件不能同时生效。
- 推荐岗位筛选默认收起，多选下拉支持点击外部关闭、选中后保持展开和滚动位置，关闭后直接显示已选内容；任务运行、限速等待、暂停、刷新重试和取消期间允许查看及展开条件，但禁止修改。
- 自动消息任务执行期间禁止修改岗位来源、目标岗位、检索关键词、仅在线、非猎头、猎聘排序及普通双击条件；双击普通条件时显示“任务执行中无法编辑”浮层提示。
- BOSS 检索 POST 请求日志新增原始 Request payload；修复直聘任务在限速等待、请求等待或环境异常刷新暂停后无法及时取消的问题，取消会唤醒等待并中止当前可取消请求。
- 猎聘岗位来源下新增“类型”单选项，默认“最新”并发送 `data.sortType=PC_HP_NEW`，选择“综合”时发送 `data.sortType=PC_HP_MIX`；同时统一直聘与猎聘岗位来源区域的紧凑行高。

## 6.0.0 - 2026-08-05

- 自动消息面板扩展为同时支持 BOSS直聘和猎聘，目标职位按站点隔离保存，并保留限速、暂停继续、取消、30 分钟自动暂停、浮动/停靠、日志和最近发送结果。
- 猎聘新增推荐岗位自动打招呼：读取有效求职期望和首页推荐岗位，复用岗位详情解析及关键词过滤后，通过 `chat.open-chat` 触发平台默认招呼语。
- 猎聘薪资、年限、在线状态和公司名称严格在详情请求前分别使用 `job.salary`、`job.requireWorkYears`、`recruiter.imStatus` 和 `comp.fullCompanyName` 筛选，详情结果不再覆盖这四项判断。
- 猎聘技术关键字、职位关键字和岗位关键字过滤器统一只检索详情页 `[data-selector="job-intro-content"]` 中的职位介绍正文，不再读取岗位标题、技能标签、“其他信息”或公司简介。
- 猎聘自动打招呼按 `recruiterId + jobId` 持久化防重历史；明确成功后写入通用沟通记录、公司资料和猎聘 IM 字段，响应未知时不自动重试。
- BOSS 自动打招呼的薪资、年限、在线状态和公司名称改为在详情请求前仅使用推荐列表的 `salaryDesc`、`jobExperience`、`bossOnline` 和 `brandName` 判断，过滤未通过的岗位不再请求详情。
- 自动消息配置移除独立编辑表单和“编辑”按钮，统一通过配置总览双击快速编辑；“仅在线”改为直接复选框，并新增“非猎头”复选框，BOSS 按 `goldHunter=1`、猎聘按 `job.jobKind=1` 在详情请求前排除猎头岗位。
- 扩展弹窗将“自动消息”移动到“查看记录总览”下方，并在“仅在线”右侧新增共享自动消息配置的“非猎头”复选框。
- 日志模式下，猎聘自动消息补齐期望、推荐、岗位详情和默认招呼语的完整请求、响应头及响应体输出；Cookie 与 XSRF Token 保持脱敏。
- 当前只处理猎聘首页推荐接口的首屏岗位；待取得下一页真实请求样本后再接入分页。

## 5.3.0 - 2026-08-03

- 扩展弹窗在“查看记录总览”下方新增按标签页保存的“仅在线”复选框，悬停提示修改后需要刷新页面生效；BOSS 和猎聘均可按标签页独立启用。
- BOSS 推荐岗位页刷新后监听 `recommend/job/list.json` 的 Fetch/XHR 响应，严格根据 `bossOnline === false` 提取离线岗位的 `encryptJobId`。
- 离线岗位 ID 与 `.job-card-box` 内链接匹配后删除整张岗位卡片，并使用 DOM 观察器持续处理无限滚动和前端重新渲染。
- 过滤后岗位不足以形成页面滚动距离时，根据 `hasMore` 自动触发 BOSS 页面自身的后续加载，并通过请求锁、超时和连续次数上限避免重复请求。
- 猎聘推荐岗位页监听 `home-recommend-job-new` 的 POST 响应，严格根据 `recruiter.imStatus === false` 提取 `job.link`，匹配并隐藏对应 `.pull-up-li` 岗位卡片，同时复用自动补量保护；保留 React 管理的 DOM 节点，避免切换求职期望时出现 `removeChild` 异常。
- 弹窗新增按标签页启用的“不显示”公司关键字过滤和持久化输入框，支持使用 `|` 分隔多个关键字；BOSS 匹配 `.boss-name`，猎聘兼容匹配构建哈希化的 `company-name-*` 类名。
- 新增“仅在线”岗位过滤修改方案，网络捕获、标签页状态和 DOM 处理均保留站点适配扩展边界。

## 5.2.1 - 2026-08-01

- 开发版本的扩展名称增加 `-dev` 后缀，正式打包时自动移除，便于区分本地版和发布版。
- 同步按钮累计点击超过 5 次后显示一次性评分提示，关闭或前往商店后不再重复显示。

## 5.2.0 - 2026-07-30

- 将扩展运行 JavaScript 全部迁移到 `src/`，同步更新 manifest、HTML、后台动态注入和打包路径；修复 `boss-message-protocol.js` 未进入发布包的问题。
- 新增 `npm run build`、`npm run check` 和 `npm run ci`，检查 JavaScript 语法以及 manifest、HTML、后台依赖和打包清单的文件完整性。
- 同步按钮累计点击超过 10 次后显示一次性评分提示；关闭或前往商店后不再打扰，计数、阈值、存储键和商店地址均使用本地状态及运行配置管理。
- “允许匿名使用统计”设置移动到结果页表格与 JSON 预览之间，默认开启；用户主动关闭后保留选择，扩展更新不会覆盖。
- 同步页将完整会话和岗位详情统计换行展示，减少单行信息过长。

## 5.1.0 - 2026-07-30

- 新增 GA4 匿名使用统计，记录安装、每日活跃、同步结果、保存记录和 CSV 下载事件，并附带插件版本、招聘平台、页面类型、操作系统、架构和 Chrome 版本等非业务维度。
- 扩展弹窗新增匿名统计开关；聊天内容、账号标识、公司岗位、Cookie、搜索关键词、完整 URL 和原始错误信息不会发送到 GA4。
- 支持正式打包时交互式填写 GA4 Measurement ID 和 API Secret，配置缺失时终止打包，也可使用 `--skip-ga4` 明确生成关闭统计的包。
- 开发模式支持从 Git 忽略的 `runtime-config.local.js` 读取本地 GA4 配置；开发事件版本自动追加 `-dev`，正式包继续使用纯数字版本。
- 商店打包阶段使用锁定版本的 esbuild 压缩 JavaScript，不生成 source map，并继续排除本地配置、源码管理文件和开发文档。

## 5.0.0 - 2026-07-30

- BOSS直聘和猎聘新增完整会话同步：分页拉取当前联系人全部文本消息，统一保存为跨平台 `conversation` 结构，并保留原有最近消息和增量同步判断逻辑。
- 总览页“更新选中”同时检查完整会话；已有完整岗位信息时只同步会话，岗位信息缺失时再补齐岗位详情。同步结果按唯一记录统计成功、失败和跳过，重试不再重复累计，并单独记录风控触发次数。
- 原消息列在完整会话可用时支持悬浮或键盘聚焦查看时间顺序对话，招聘者显示在左侧、当前用户显示在右侧；会话框主要向左展开，减少遮挡。
- JSON 预览改为“沟通记录、公司信息、个人信息”Tab；个人信息仅在 Debug 模式显示，沟通记录仅展示当前分页，非 Debug 模式隐藏 `companyKey`、`jobRef` 和 `jobInfo`。
- 总览页新增分页和每页数量设置，默认每页 100 条；关键词始终基于全部记录查询并对完整查询结果分页，当前页全选和跨页选择状态保持一致。
- CSV 下载在存在选中记录时显示选中数量并仅导出选中项，没有选中记录时导出全部记录。
- 猎聘发送消息成功后会将旧完整会话标记为待重新同步；BOSS直聘继续保持相同的会话失效规则。
- 新增 BOSS直聘与猎聘完整会话同步方案，并更新猎聘说明和通用数据模型文档。

## 4.0.0 - 2026-07-30

- 猎聘新增批量发送纯文本消息：同步保存 `oppositeUserId`，旧记录发送前通过聊天列表补全；按账号缓存 `imClientId`，支持限速、停止、逐条状态、失败隔离及成功后的本地消息回写。
- 总览页发送入口改为同时支持 BOSS 和猎聘，同一批次必须选择同一个招聘网站；发送速率按站点分别保存。
- 猎聘普通同步不再仅因旧记录缺少 `oppositeUserId` 就计入待更新；消息、岗位本来需要更新时顺带保存，手动同步岗位和发送消息时按需补全，并修正待更新总数与新增、更新、岗位详情分类不一致的问题。
- 批量发送日志改为仅在当前结果页内存中一次性展示，不再写入浏览器存储；日志框支持聚焦后使用 `Ctrl/Command+A` 全选内容。
- 请求日志按钮仅在同步结果页开启 `log` 模式时显示；请求日志、岗位日志和发送日志均支持聚焦后全选。
- 总览页公司列新增悬浮详情卡，从已同步的公司资料中展示名称、行业、规模和介绍。
- 移除“下载 JSON”按钮及相关代码，保留 JSON 预览和复制功能。
- 新增猎聘发送协议专题文档，并更新 README、猎聘说明和数据模型文档。

## 3.1.0 - 2026-07-30

- 岗位同步日志改为通过运行时消息一次性展示在当前结果页内存中，不再写入 `chrome.storage.local` 或提供历史日志；补充任务派发、标签页状态、耗时及扩展上下文失效诊断信息。
- 总览页新增 `debug` 数据模式：开启后 JSON 和 CSV 导出包含完整内部数据，CSV 导入支持按唯一索引新增记录或覆盖已有内部字段；普通模式继续只处理公开字段。
- 开发版在结果页标题右侧新增 `log`、`debug` 复选框并默认开启，可通过复选框或 URL 参数覆盖；打包版隐藏复选框且默认关闭，但仍支持 `log=true`、`debug=true` 临时启用。
- BOSS 岗位同步改为先查询 `geekFilterByLabel` 校验当前联系人关系，仅请求命中的关系 ID；目标不在近期联系人列表、联系人当前岗位已变化或详情岗位与原记录不一致时，保留原记录并以“最近沟通时间超过30天，无法获取详情”成功结束。
- 区分 BOSS 联系人关系 ID `relationFriendId` 与发送所需招聘者用户 ID `friendId`，修复同一招聘者存在多个岗位时可能匹配并覆盖错误岗位的问题。
- 已保存的显式 `recordKey` 现在始终作为唯一标识优先使用；岗位同步前会规范化旧记录，并分别提示缺失记录和重复唯一标识，避免笼统的“部分选中记录已不存在”错误。
- 更新 BOSS、猎聘、数据模型、岗位同步及批量发送文档，使日志生命周期、内部数据边界和旧记录终止状态与当前实现保持一致。

## 3.0.0 - 2026-07-29

- 完成通用岗位详情同步重构：新增站点适配器与统一同步核心；同步页使用缺失补齐策略，总览页可对选中记录强制刷新，BOSS 直聘优先接入，猎聘保留岗位能力扩展点。
- BOSS 岗位详情改用 `getBossData` 后的 `wapi/zpgeek/job/detail.json` JSON 接口；岗位数据统一保存为 `jobRef`、`jobInfo`，公司资料独立保存并通过 `companyKey` 关联。
- 岗位详情请求固定按 2 秒间隔调度，每完成 4 次请求自动刷新最近使用的 BOSS 标签页；刷新期间阻塞后续请求，并在页面恢复后使用新的运行时请求上下文继续。
- `code=37` 风控响应改为刷新 BOSS 标签页后按可配置的延时和次数重试；支持立即暂停当前请求、刷新等待和重试倒计时，已成功记录不会重做。
- BOSS 返回 `code=200301`“该职位已不存在”时，清空岗位内容并标记为已同步，保留说明且不中断后续同步。
- 同步页新增新增/更新选择、沟通记录与岗位信息分类进度；首次待更新列表会保存为快照，暂停、重试和刷新后均从原列表的剩余记录继续，进度保持累计一致。
- 总览页岗位更新新增暂停/继续、重试延时和次数设置、逐条状态、刷新提示及完整请求日志；停止状态关闭弹窗会退出本次更新并清空选中记录。
- JSON 输出使用通用岗位结构；CSV 保留“内部数据”以恢复聊天同步和发送标识，但明确排除岗位信息，导入时按唯一索引增量合并并保护已有岗位字段。
- 新增数据模型文档和岗位详情重构方案文档，说明记录、导入导出、内部存储、同步快照、公司资料与敏感字段边界。

## 2.0.0 - 2026-07-24

- 扩展名称由“直聘|猎聘 沟通记录统计器”更新为“直聘|猎聘 沟通助手”，工具栏标题同步调整，版本升级为 2.0.0。
- 总览页新增按公司、岗位、招聘者和原消息的延迟查询，支持多关键词 AND 匹配，并与现有筛选、排序组合生效。
- 消息状态筛选统一为全部、未读和已读；无法识别的状态按未读展示和筛选。
- 总览页新增“发送信息”批量发送弹窗：显示全部目标及实时状态；发送状态统一为“成功、失败、等待”，最右侧备注栏显示失败原因。
- 默认每分钟发送 10 条，仅保留最小值 1，不再限制最大值；发送期间按钮变为“停止”，停止后不再发送后续目标。
- 支持向当前已登录 BOSS直聘账号的既有联系人发送纯文本消息；猎聘记录会阻止整个批次，BOSS 记录标识不全时自动尝试补齐。
- 新增页面主世界 WebSocket 协议编解码、认证、注册、推送 ACK、文本 ACK 校验与 ACK 超时保护；认证凭据只保留在页面内存中。
- BOSS 联系人数据拆分 ownerUserId、friendId、peerKey、chatSecurityId 和 uploadSecurityId，旧 securityId 仅兼容记录索引，不再作为发送凭据。
- 发送弹窗新增完整流程日志，记录自动补全、HTTP 请求地址、参数和返回值、WebSocket 生命周期及逐条发送结果，认证和联系人凭据自动脱敏；日志栏默认隐藏，仅在结果页使用 `log=enable` 参数时显示。
- 发送期 HTTP 日志现记录请求方法、路径、headers、body 参数与响应（认证和联系人凭据自动脱敏）；`pc_device_id` 从页面初始化请求、本地运行状态或页面自身 WebSocket 注册帧捕获，不再依赖 toggle 接口响应。
- 已保存有效 friendId 和 peerKey 的目标会直接进入发送，不再扫描联系人列表或重复调用 getBossData；仅为信息不全的选中记录查询联系人并把稳定字段回写总记录。
- 自动补全通过 recordKey、peerKey、friendId 和 jobId 精确匹配目标，仅对匹配的 friendId 请求详情；补全接口或单条发送失败不会阻断其他有效记录。
- 文本消息收到成功 ACK 后会更新对应总记录的原消息和更新时间，并将消息状态设为未读；失败记录不会修改总记录中的消息。
- 登录态检查改为在选中的 BOSS 标签页页面主世界请求用户信息，确保使用网页自身 Cookie，避免内容脚本请求未携带登录态而误报“未登录”。
- 发送时只选择最近访问的 BOSS 标签页并检查该页账号；内容脚本不存在时自动注入并重试，等待页面 Hook 就绪后再启动批次。
- BOSS 协议 Hook 改为在页面主世界 document_start 加载；职位页未建立原生聊天连接时，使用扩展为当前浏览器持久化的稳定 pc_device_id，不再要求反复刷新捕获注册帧。
- 发送弹窗的联系人校验与后端预检保持一致：有效 friendId+peerKey 或可精确定位 peerKey 的 recordKey 均可发送，纯文本发送不再要求 chatSecurityId。
- peerKey 校验兼容 BOSS 实际返回的 `~` 字符，修复合法 recordKey 被误判为缺少联系人标识的问题。
- 目标补齐支持 encryptFriendId 调用 getBossData，并扩展数字 friendId 的权威候选来源；失败日志会分别显示 friendId/peerKey 的有效性与长度。

## 1.39.0 - 2026-06-09

- 同步页限速从“每秒 N 条”升级为“每 时/分/秒 同步 N 条”，按所选周期均匀分布请求。
- BOSS直聘和猎聘同步共同使用新的限速配置，并兼容旧版每秒同步设置。
- 更新同步准备提示文案，避免继续强调固定每秒限速。
- 同步页新增请求日志入口，便于查看最近的接口调用情况。
- 记录表新增消息状态筛选和展示，导入导出同步保留状态字段。
- 同步统计区分新增记录和消息更新，BOSS直聘同步会在消息或读取状态变化时更新记录。

## 1.38.0 - 2026-06-07

- 修复结果页继续报 `escapeHtml is not defined` 的问题，将 HTML 转义方法补回公共工具。
- 移除结果页旧版 BOSS 消息页刷新错误分支，避免重构后遗留的未定义函数引用。
- 补充页面脚本静态检查，覆盖重构后可能遗漏的全局 helper。

## 1.37.0 - 2026-06-07

- 修复结果页同步渲染时报 `boldNumber is not defined` 的问题。
- 恢复同步页和总览页统计数字加粗展示，避免页面加载中断。

## 1.36.0 - 2026-06-07

- 新增 Chrome 商店上传包脚本，仅打包扩展运行必需的 manifest、脚本、页面和图标文件。
- 打包输出固定写入 `dist/job-chat-recorder-v{version}.zip`，并自动使用 `manifest.json` 中的版本号。
- 更新 README，补充商店包生成方式和产物说明。

## 1.35.0 - 2026-06-07

- 按领域重构代码结构，拆分公共工具、记录规范、数据库访问、页面控制、BOSS直聘抓取和猎聘抓取。
- BOSS直聘和猎聘抓取模块统一暴露 `prepare` / `extract` 接口，降低平台逻辑耦合。
- 结果页导入、导出和本地存储访问迁移到数据库模块，页面脚本聚焦展示和按钮交互。

## 1.34.0 - 2026-06-07

- 修复同步页忽略选中后记录可能仍显示的问题，忽略后会立即从同步列表移除。
- 同步页加载时会按忽略记录表再次过滤 pending 记录，防止刷新或 storage 事件后回显。

## 1.33.0 - 2026-06-07

- 同步页和总览页新增“忽略选中”，选中记录会移动到本地忽略记录表。
- 同步前会检查忽略记录表，已忽略的记录不会再次同步。
- 总览页新增“忽略记录”弹窗，可查看忽略记录并恢复到记录表。
- 记录表和忽略表保持互斥，同一唯一索引只会存在于其中一张表。

## 1.32.0 - 2026-06-04

- BOSS直聘同步不再要求当前页面必须是消息页，支持在 zhipin.com 登录态页面直接通过接口获取数据。
- 删除“打开/刷新 BOSS 消息页”相关按钮和旧捕获提示逻辑。

## 1.31.0 - 2026-06-04

- BOSS直聘唯一索引改为 `boss|encryptBossId|jobId`，支持同一个招聘者关联多个岗位。
- BOSS直聘同步前如果记录已存在但 `lastMessageInfo.msgId` 变化，会重新同步该记录。
- BOSS直聘消息变化时使用 `lastMessageInfo.msgTime` 更新“更新时间”，并更新“原消息”。

## 1.30.0 - 2026-06-03

- 修复猎聘待同步数量判断，去重比较同时兼容裸 `oppositeImId` 和 `liepin|oppositeImId`。
- 猎聘同步前会先和已保存记录、待保存记录进行统一 key 对比，已存在联系人不再重复同步。

## 1.29.0 - 2026-06-03

- 修复 BOSS直聘待同步数量过多的问题，去重逻辑恢复旧版唯一索引优先级。
- BOSS直聘唯一索引继续优先使用 `encryptBossId/encryptUid/uid/jobId/msgId`，`friendId` 仅作为兜底。
- 去重比较同时兼容裸 id 和 `boss|id` 形式，并保留导入数据已有的唯一索引。

## 1.28.0 - 2026-06-03

- BOSS直聘同步改为先通过 `geekFilterByLabel?labelId=0` 获取联系人 `friendId`。
- 使用获取到的 `friendId` 拼装 `friendIds` payload，再请求 `getGeekFriendList.json` 获取具体岗位信息。
- 合并标签列表中的联系人标识和更新时间，保留新接口去重字段并使用旧接口岗位详情。

## 1.27.0 - 2026-06-03

- BOSS直聘联系人列表改为通过 `geekFilterByLabel?labelId=0` 接口获取。
- 适配新接口返回的 `friendList`、`updateTime`、`encryptFriendId` 和 `friendId` 字段。
- 更新 BOSS 页面接口 hook，改为捕获新列表接口的 GET 响应。
